RSpec.describe Rspec::ExpectChanges do
  it "has a version number" do
    expect(Rspec::ExpectChanges::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
